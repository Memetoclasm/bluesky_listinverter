{
  "name": "bsky-list-converter",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@atproto/api": "^0.19.4",
    "@atproto/oauth-client-browser": "^0.3.41"
  },
  "devDependencies": {
    "jsdom": "^29.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.0"
  }
}
